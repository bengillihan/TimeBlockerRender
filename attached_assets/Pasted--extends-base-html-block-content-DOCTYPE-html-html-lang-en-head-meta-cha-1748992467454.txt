{% extends "base.html" %}

{% block content %}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>TimeBlocker</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
    <link href="{{ url_for('static', filename='css/style.css') }}" rel="stylesheet">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container-fluid">
            <a class="navbar-brand" href="/">TimeBlocker</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav me-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="#" id="newTaskBtn" title="New Task (Alt + N)">
                            <i class="fas fa-plus"></i> New Task
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#" id="newTodoBtn" title="New Todo (Alt + T)">
                            <i class="fas fa-tasks"></i> New Todo
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#" id="quickAddBtn" title="Quick Add (Alt + Q)">
                            <i class="fas fa-bolt"></i> Quick Add
                        </a>
                    </li>
                </ul>
                <div class="d-flex">
                    <button class="btn btn-outline-light me-2" id="saveBtn" title="Save (Alt + S)">
                        <i class="fas fa-save"></i> Save
                    </button>
                    <a href="{{ url_for('logout') }}" class="btn btn-outline-light">
                        <i class="fas fa-sign-out-alt"></i> Logout
                    </a>
                </div>
            </div>
        </div>
    </nav>

    <div class="container-fluid mt-4">
        <div class="row">
            <!-- Time Blocks Section -->
            <div class="col-md-8">
                <div class="card">
                    <div class="card-header d-flex justify-content-between align-items-center">
                        <h5 class="mb-0">Time Blocks</h5>
                        <div class="btn-group">
                            <button class="btn btn-sm btn-outline-secondary" id="prevDay">
                                <i class="fas fa-chevron-left"></i>
                            </button>
                            <button class="btn btn-sm btn-outline-secondary" id="today">Today</button>
                            <button class="btn btn-sm btn-outline-secondary" id="nextDay">
                                <i class="fas fa-chevron-right"></i>
                            </button>
                        </div>
                    </div>
                    <div class="card-body">
                        <div class="time-blocks-container">
                            {% for hour in range(day_start.hour, day_end.hour + 1) %}
                                {% for minute in [0, 15, 30, 45] %}
                                    <div class="time-slot" data-time="{{ '%02d:%02d'|format(hour, minute) }}">
                                        <div class="time-label">{{ '%02d:%02d'|format(hour, minute) }}</div>
                                        <div class="time-block-container">
                                            {% for block in time_blocks %}
                                                {% if block.start_time == '%02d:%02d'|format(hour, minute) %}
                                                    <div class="time-block" id="block-{{ block.id }}" draggable="true">
                                                        {{ block.title }}
                                                    </div>
                                                {% endif %}
                                            {% endfor %}
                                        </div>
                                    </div>
                                {% endfor %}
                            {% endfor %}
                        </div>
                    </div>
                </div>
            </div>

            <!-- Tasks and Todos Section -->
            <div class="col-md-4">
                <div class="card mb-4">
                    <div class="card-header">
                        <h5 class="mb-0">Tasks</h5>
                    </div>
                    <div class="card-body">
                        <div class="task-list">
                            {% for task in tasks.items %}
                                <div class="task-item" data-task-id="{{ task.id }}">
                                    <div class="form-check">
                                        <input class="form-check-input" type="checkbox" 
                                               {% if task.completed %}checked{% endif %}>
                                        <label class="form-check-label">
                                            {{ task.title }}
                                        </label>
                                    </div>
                                    <div class="task-meta">
                                        <span class="badge bg-{{ task.priority }}">{{ task.priority }}</span>
                                        {% if task.due_date %}
                                            <span class="due-date">{{ task.due_date.strftime('%Y-%m-%d') }}</span>
                                        {% endif %}
                                    </div>
                                </div>
                            {% endfor %}
                        </div>
                        {% if tasks.pages > 1 %}
                            <nav aria-label="Task pagination">
                                <ul class="pagination justify-content-center">
                                    {% for page in range(1, tasks.pages + 1) %}
                                        <li class="page-item {% if page == tasks.page %}active{% endif %}">
                                            <a class="page-link" href="?page={{ page }}">{{ page }}</a>
                                        </li>
                                    {% endfor %}
                                </ul>
                            </nav>
                        {% endif %}
                    </div>
                </div>

                <div class="card">
                    <div class="card-header">
                        <h5 class="mb-0">Todos</h5>
                    </div>
                    <div class="card-body">
                        <div class="todo-list">
                            {% for todo in todos.items %}
                                <div class="todo-item" data-todo-id="{{ todo.id }}">
                                    <div class="form-check">
                                        <input class="form-check-input" type="checkbox"
                                               {% if todo.completed %}checked{% endif %}>
                                        <label class="form-check-label">
                                            {{ todo.title }}
                                        </label>
                                    </div>
                                    <div class="todo-meta">
                                        <span class="badge bg-{{ todo.priority }}">{{ todo.priority }}</span>
                                        {% if todo.due_date %}
                                            <span class="due-date">{{ todo.due_date.strftime('%Y-%m-%d') }}</span>
                                        {% endif %}
                                    </div>
                                </div>
                            {% endfor %}
                        </div>
                        {% if todos.pages > 1 %}
                            <nav aria-label="Todo pagination">
                                <ul class="pagination justify-content-center">
                                    {% for page in range(1, todos.pages + 1) %}
                                        <li class="page-item {% if page == todos.page %}active{% endif %}">
                                            <a class="page-link" href="?page={{ page }}">{{ page }}</a>
                                        </li>
                                    {% endfor %}
                                </ul>
                            </nav>
                        {% endif %}
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Quick Add Modal -->
    <div class="quick-add-modal" id="quickAddModal">
        <div class="quick-add-content">
            <h5>Quick Add</h5>
            <form id="quickAddForm">
                <div class="mb-3">
                    <label class="form-label">Type</label>
                    <select class="form-select" name="type" required>
                        <option value="task">Task</option>
                        <option value="todo">Todo</option>
                    </select>
                </div>
                <div class="mb-3">
                    <label class="form-label">Title</label>
                    <input type="text" class="form-control" name="title" required>
                </div>
                <div class="mb-3">
                    <label class="form-label">Description</label>
                    <textarea class="form-control" name="description"></textarea>
                </div>
                <div class="mb-3">
                    <label class="form-label">Due Date</label>
                    <input type="datetime-local" class="form-control" name="due_date">
                </div>
                <div class="mb-3">
                    <label class="form-label">Priority</label>
                    <select class="form-select" name="priority">
                        <option value="low">Low</option>
                        <option value="medium" selected>Medium</option>
                        <option value="high">High</option>
                        <option value="urgent">Urgent</option>
                    </select>
                </div>
                <div class="text-end">
                    <button type="button" class="btn btn-secondary" onclick="document.getElementById('quickAddModal').classList.remove('show')">Cancel</button>
                    <button type="submit" class="btn btn-primary">Add</button>
                </div>
            </form>
        </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="{{ url_for('static', filename='js/enhancements.js') }}"></script>
</body>
</html>
{% endblock %}